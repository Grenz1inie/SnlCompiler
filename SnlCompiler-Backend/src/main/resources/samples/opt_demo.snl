program optdemo
var integer a, b, c, d;
begin
    { 1. 常量折叠：1+2+10 编译期算成 13 }
    a := 1 + 2 + 10;

    { 2. 代数化简：a+0 等价于 a }
    b := a + 0;

    { 3. 复制传播：b*1 等价于 b，临时变量别名会被替换 }
    c := b * 1;

    { 4. 死代码消除：d 的中间计算 9+8 折叠后若临时变量未被引用会被删除 }
    d := 9 + 8;

    write(c)
end.
